 UPDATE EMPLOYEE SET SUPER_SSN=103,DNO=1 WHERE SSN=103;
 
 ALTER TABLE EMPLOYEE ADD FOREIGN KEY(DNO) REFERENCES DEPARTMENT(DNUMBER)
 
  UPDATE EMPLOYEE SET ADDRESS='KASHMIR' WHERE SSN=105;
  
  ALTER TABLE EMPLOYEE MODIFY SALARY INT;
  
  DROP TABLE DIP;
  
  DELETE FROM DUSK WHERE ROLL=2;
 
  RENAME TABLE DUSK TO DAWN;
  
  ALTER TABLE DAWN CHANGE ROLL ID INT;
  
  SELECT DISTINCT(SALARY) FROM EMPLOYEE;